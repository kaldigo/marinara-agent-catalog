# Custom Packages

Store our Marinara capability packages here on the `agents` branch.

Each package should live in `custom-packages/<package-id>/` and include at least:

- `manifest.json`
- every file referenced by `manifest.entrypoints`

The `main` branch is generated by GitHub Actions. Do not hand-edit generated catalog
files on `main`; change package source here instead.
