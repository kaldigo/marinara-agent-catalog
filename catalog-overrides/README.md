# Catalog Overrides

Optional per-package catalog metadata belongs here.

Create `catalog-overrides/<package-id>.json` to override generated catalog fields,
for example:

```json
{
  "category": "utility",
  "iconUrl": "https://example.com/icon.png",
  "documentationUrl": "https://example.com/docs"
}
```
