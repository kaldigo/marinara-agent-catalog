{
  "name": "marinara-group-sort-order-package",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "description": "Package-era Group Sort Order source for Marinara Engine.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs && npm run build && node --check dist/package/client.js && node scripts/validate-dist.mjs"
  }
}
