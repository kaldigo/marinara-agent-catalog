{
  "name": "marinara-gm-notes-package",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Focused GM continuity ledger for Marinara Engine.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs && npm run build && node scripts/validate-dist.mjs"
  }
}
