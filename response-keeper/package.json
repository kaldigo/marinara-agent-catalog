{
  "name": "marinara-response-keeper-package",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Preserves stopped regenerations, stopped continuations, and manual edits.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "npm run build && node --check dist/package/client.js && node scripts/validate-dist.mjs"
  }
}
