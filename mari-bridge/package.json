{
  "name": "marinara-mari-bridge-package",
  "version": "1.0.25",
  "private": true,
  "type": "module",
  "description": "Installer for the injected Mari Bridge runtime used by Marinara capability packages.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs && npm run build && node scripts/validate-dist.mjs"
  }
}
