{
  "name": "marinara-mari-bridge",
  "version": "1.4.0",
  "private": true,
  "type": "module",
  "description": "Shared SDK for Marinara capability packages that depend on Mari Bridge.",
  "scripts": {
    "check": "node --check sdk/index.js && node --check sdk/contracts.js && node --check sdk/server.js && node --check sdk/client.js && node scripts/check-sdk.mjs"
  }
}
